# CMake package configuration file for the @PROJECT_NAME@ module

@PACKAGE_GUARD@
@PACKAGE_INIT@

set(SOFANONUNIFORMFEM_HAVE_SOFADENSESOLVER @SOFANONUNIFORMFEM_HAVE_SOFADENSESOLVER@)

find_package(SofaBase QUIET REQUIRED)
find_package(SofaGeneralSimpleFem QUIET REQUIRED)

if(SOFANONUNIFORMFEM_HAVE_SOFADENSESOLVER)
    find_package(SofaDenseSolver QUIET REQUIRED)
endif()

find_package(Sofa.Component.Topology.Grid QUIET REQUIRED)
find_package(Sofa.Component.Topology.DynamicContainer QUIET REQUIRED)

message(WARNING "@PROJECT_NAME@ module is being deprecated; you may need to use Sofa.Component.Topology.Grid and Sofa.Component.Topology.DynamicContainer.")

if(NOT TARGET @PROJECT_NAME@)
    include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
endif()

check_required_components(@PROJECT_NAME@)
