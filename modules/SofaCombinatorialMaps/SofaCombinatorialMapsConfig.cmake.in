# CMake package configuration file for @PROJECT_NAME@
@PACKAGE_INIT@

find_package(Newmat QUIET REQUIRED)
find_package(cgogn_core QUIET REQUIRED)
find_package(cgogn_geometry QUIET REQUIRED)
find_package(cgogn_io QUIET REQUIRED)
find_package(cgogn_modeling QUIET REQUIRED)

if(NOT TARGET @PROJECT_NAME@)
    include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
endif()

check_required_components(@PROJECT_NAME@)
