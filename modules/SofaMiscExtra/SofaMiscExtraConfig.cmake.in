# CMake package configuration file for the SofaMiscExtra module
@PACKAGE_INIT@

find_package(SofaMiscTopology REQUIRED)
find_package(SofaGeneral REQUIRED)

### Is the target existing ?
if(NOT TARGET SofaMiscExtra)
    ### If not we are building out of tree.
    include("${CMAKE_CURRENT_LIST_DIR}/SofaMiscExtraTargets.cmake")
endif()

check_required_components(SofaMiscExtra)
