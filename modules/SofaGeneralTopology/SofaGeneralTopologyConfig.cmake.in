# CMake package configuration file for the @PROJECT_NAME@ module

@PACKAGE_GUARD@
@PACKAGE_INIT@

find_package(Sofa.Component.Topology.Grid QUIET REQUIRED)
find_package(Sofa.Component.Topology.StaticContainer QUIET REQUIRED)

message(WARNING "@PROJECT_NAME@ module is deprecated, use Sofa.Component.Topology.Grid and Sofa.Component.Topology.StaticContainer instead.")

if(NOT TARGET @PROJECT_NAME@)
    include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
endif()

check_required_components(@PROJECT_NAME@)
