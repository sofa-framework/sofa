# CMake package configuration file for the @PROJECT_NAME@ module

@PACKAGE_GUARD@
@PACKAGE_INIT@

find_package(SofaGeneralSimpleFem QUIET REQUIRED)
find_package(SofaGeneralTopology QUIET REQUIRED)
find_package(Sofa.Component.Topology.Mapping QUIET REQUIRED)

message(WARNING "@PROJECT_NAME@ module is being deprecated, you may need to use Sofa.Component.Topology.Mapping instead.")

if(NOT TARGET @PROJECT_NAME@)
    include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
endif()

check_required_components(@PROJECT_NAME@)
