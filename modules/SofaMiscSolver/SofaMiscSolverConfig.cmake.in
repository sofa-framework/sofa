# CMake package configuration file for the SofaMiscSolver module
@PACKAGE_INIT@


find_package(SofaSimulation REQUIRED)

### Is the target existing ?
if(NOT TARGET SofaMiscSolver)
    ### If not we are building out of tree.
    include("${CMAKE_CURRENT_LIST_DIR}/SofaMiscSolver.cmake")
endif()

check_required_components(SofaMiscSolver)
