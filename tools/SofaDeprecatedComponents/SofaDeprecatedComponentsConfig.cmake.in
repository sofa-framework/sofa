# CMake package configuration file for SofaDeprecatedComponents

@PACKAGE_INIT@

find_package(SofaFramework REQUIRED)
find_package(SofaGeneral REQUIRED)

if(NOT TARGET SofaDeprecatedComponents)
    include("${CMAKE_CURRENT_LIST_DIR}/SofaDeprecatedComponentsTargets.cmake")
endif()

check_required_components(SofaDeprecatedComponents)
