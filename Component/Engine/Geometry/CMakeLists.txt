cmake_minimum_required(VERSION 3.12)
project(Sofa.Component.Engine.Geometry LANGUAGES CXX)

set(SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR "src/sofa/component/engine/geometry")

set(HEADER_FILES
    ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/config.h.in
    ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/init.h
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/Distances.h
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/Distances.inl
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/HausdorffDistance.h
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/HausdorffDistance.inl
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/NormalsFromPoints.h
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/NormalsFromPoints.inl
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/NormEngine.h
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/NormEngine.inl
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/ShapeMatching.h
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/ShapeMatching.inl
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/RandomPointDistributionInSurface.h
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/RandomPointDistributionInSurface.inl
)

set(SOURCE_FILES
    ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/init.cpp
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/Distances.cpp # needs DynamicSparseGeoAlgo
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/HausdorffDistance.cpp
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/NormalsFromPoints.cpp
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/NormEngine.cpp
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/ShapeMatching.cpp
    # ${SOFACOMPONENTENGINEGEOMETRY_SOURCE_DIR}/RandomPointDistributionInSurface.cpp
)

sofa_find_package(SofaSimulationCore REQUIRED)

add_library(${PROJECT_NAME} SHARED ${HEADER_FILES} ${SOURCE_FILES})
target_link_libraries(${PROJECT_NAME} PUBLIC SofaSimulationCore)

sofa_create_package_with_targets(
    PACKAGE_NAME ${PROJECT_NAME}
    PACKAGE_VERSION ${Sofa_VERSION}
    TARGETS ${PROJECT_NAME} AUTO_SET_TARGET_PROPERTIES
    INCLUDE_SOURCE_DIR "src"
    INCLUDE_INSTALL_DIR "${PROJECT_NAME}"
)
