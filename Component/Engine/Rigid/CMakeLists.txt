cmake_minimum_required(VERSION 3.12)
project(Sofa.Component.Engine.Rigid LANGUAGES CXX)

set(SOFACOMPONENTENGINERIGID_SOURCE_DIR "src/sofa/component/engine/rigid")

set(HEADER_FILES
    ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/config.h.in
    ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/init.h
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/QuatToRigidEngine.h
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/QuatToRigidEngine.inl
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/RigidToQuatEngine.h
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/RigidToQuatEngine.inl
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/Vertex2Frame.h
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/Vertex2Frame.inl
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/GenerateRigidMass.h
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/GenerateRigidMass.inl
)

set(SOURCE_FILES
    ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/init.cpp
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/QuatToRigidEngine.cpp
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/RigidToQuatEngine.cpp
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/Vertex2Frame.cpp
    # ${SOFACOMPONENTENGINERIGID_SOURCE_DIR}/GenerateRigidMass.cpp
)

sofa_find_package(SofaSimulationCore REQUIRED)

add_library(${PROJECT_NAME} SHARED ${HEADER_FILES} ${SOURCE_FILES})
target_link_libraries(${PROJECT_NAME} PUBLIC SofaSimulationCore)

sofa_create_package_with_targets(
    PACKAGE_NAME ${PROJECT_NAME}
    PACKAGE_VERSION ${Sofa_VERSION}
    TARGETS ${PROJECT_NAME} AUTO_SET_TARGET_PROPERTIES
    INCLUDE_SOURCE_DIR "src"
    INCLUDE_INSTALL_DIR "${PROJECT_NAME}"
)
