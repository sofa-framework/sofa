Node : {
    name : "root"

    Node : {
        name : "child"
        Python : '''
                 for i in range(0,3):
                     self.createChild("child"+str(i))
                 '''

        Python : '''
                 for i in range(0,3):
                    root.createChild("root"+str(i))
                 '''
    }
}
